#include <stdio.h>

int main() {
    int input = 5;  // Static input
    if (input > 3) {
        printf("Input is greater than 3\n");
    } else {
        printf("Input is not greater than 3\n");
    }
    return 0;
}
