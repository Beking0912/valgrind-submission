#include <stdio.h>

int main() {
    int input;
    scanf("%d", &input); // User input

    if (input > 3) {
        printf("Input is greater than 3\n");
    } else {
        printf("Input is not greater than 3\n");
    }
    return 0;
}
